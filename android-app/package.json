{
  "name": "csir-note-sheet-android",
  "version": "1.0.0",
  "private": true,
  "description": "Android wrapper (Capacitor) for the CSIR Note Sheet Generator",
  "scripts": {
    "build:web": "node build-web.mjs",
    "sync": "cap sync android",
    "build:apk": "npm run build:web && cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2",
    "esbuild": "^0.23.0",
    "postcss": "^8.4.41",
    "tailwindcss": "^3.4.10"
  }
}
